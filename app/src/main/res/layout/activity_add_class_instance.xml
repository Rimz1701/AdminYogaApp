<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Date Display -->
        <TextView
            android:id="@+id/tvPickedDate"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="No date selected"
            android:textSize="16sp"
            android:minHeight="48dp"
            android:padding="12dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Pick Date Button -->
        <Button
            android:id="@+id/btnPickDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Pick Date"
            android:minHeight="48dp"
            android:layout_marginTop="8dp"
            app:layout_constraintTop_toBottomOf="@id/tvPickedDate"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Teacher Name -->
        <EditText
            android:id="@+id/editTeacher"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:hint="Teacher Name"
            android:minHeight="48dp"
            android:padding="12dp"
            android:layout_marginTop="16dp"
            app:layout_constraintTop_toBottomOf="@id/btnPickDate"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Comments -->
        <EditText
            android:id="@+id/editComments"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:hint="Comments (optional)"
            android:inputType="textMultiLine"
            android:minHeight="48dp"
            android:padding="12dp"
            android:layout_marginTop="16dp"
            app:layout_constraintTop_toBottomOf="@id/editTeacher"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Save Button -->
        <Button
            android:id="@+id/btnSaveInstance"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Save Instance"
            android:minHeight="48dp"
            android:layout_marginTop="24dp"
            app:layout_constraintTop_toBottomOf="@id/editComments"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/btnClearInstance"
            app:layout_constraintHorizontal_weight="1" />

        <!-- Clear Button -->
        <Button
            android:id="@+id/btnClearInstance"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Clear"
            android:minHeight="48dp"
            android:layout_marginTop="24dp"
            app:layout_constraintTop_toBottomOf="@id/editComments"
            app:layout_constraintStart_toEndOf="@id/btnSaveInstance"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_weight="1" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>
